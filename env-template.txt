# Configurações do Supabase (opcional se usar upload na VPS)
SUPABASE_JWT_SECRET=seu-jwt-secret-aqui

# Armazenamento de imagens na VPS
# Pasta onde as imagens serão salvas (path absoluto na VPS ou relativo ao projeto)
UPLOAD_DIR=/app/uploads
# URL base da API (para montar URLs das imagens). Ex: https://api.seudominio.com
BASE_URL=https://api.seudominio.com
# Tamanho máximo por arquivo em MB (padrão 5)
UPLOAD_MAX_FILE_SIZE_MB=5

# MongoDB local (rodando no docker-compose)
MONGO_DB_NAME=node-user-api
MONGO_ROOT_USERNAME=app_admin
MONGO_ROOT_PASSWORD=troque-esta-senha
MONGODB_URI=mongodb://app_admin:troque-esta-senha@mongo:27017/node-user-api?authSource=admin

# Configurações do Admin Inicial
# O sistema criará automaticamente um usuário admin na primeira inicialização
# se essas variáveis estiverem configuradas e não existir nenhum admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=senha_segura_aqui
ADMIN_NAME=Administrador

# Configurações do SendGrid
# API Key do SendGrid para envio de emails
# Obtenha sua chave em: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.sua-chave-api-sendgrid-aqui
# Email remetente (deve ser verificado no SendGrid)
SENDGRID_FROM_EMAIL=noreply@yourapp.com
# Nome da aplicação (usado nos emails)
APP_NAME=Área Hub
# URL base da aplicação (usado nos links de verificação)
APP_URL=https://seu-app.onrender.com

# URL do frontend (para configuração de CORS)
FRONTEND_URL=https://react-frontend-vihi.onrender.com

# Outras variáveis de ambiente (ajuste conforme necessário)
# JWT_SECRET=sua-chave-secreta-aqui
# JWT_EXPIRES_IN=7d
# PORT=3000
# NODE_ENV=production

